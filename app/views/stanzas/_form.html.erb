<%= form_with(model: stanza, local: true) do |form| %>
  <% if stanza.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stanza.errors.count, "error") %> prohibited this stanza from being saved:</h2>

      <ul>
      <% stanza.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
